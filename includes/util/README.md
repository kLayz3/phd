#### Small utility library files

As the project grows bigger, some small hack files becomes large and there are many of them.
Here we stash helper classes that will never get serialized, i.e. don't require a dictionary.

Dependencies can be whatever, and will link back to the main `libStructures.so`
